# Ablog Backend API

See [README_BACKEND.md](../README_BACKEND.md) in the root directory for full documentation.

## Quick Start

1. Install dependencies: `npm install`
2. Create `.env` file (see `.env.example`)
3. Start MongoDB
4. Run: `npm run dev`

